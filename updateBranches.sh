git checkout step0 && git merge master -m "Merge from master" && git push
git checkout step3 && git merge master -m "Merge from master" && git push
git checkout step4 && git merge master -m "Merge from master" && git push
git checkout step5 && git merge master -m "Merge from master" && git push
git checkout master && git push
